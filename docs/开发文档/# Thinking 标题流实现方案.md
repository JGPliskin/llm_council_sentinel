# Thinking 标题流实现方案
> 版本：v1.0 | 状态：Ready to Implement

## 目录
1. 用户场景与需求描述
2. 目标与非目标
3. 详细技术方案
4. 技术架构图与流程图
5. 前端设计方案
6. 后端实现方案
7. 需要改动的代码文件与关键代码修改
8. 风险与对策

## 1. 用户场景与需求描述

### 1.1 典型用户场景
| 场景 | 用户感知 | 期望体验 |
| :--- | :--- | :--- |
| 模型推理 20–60s | 看到空白、误以为卡死 | 持续出现“思考标题”证明系统在工作 |
| 首个标题迟到 | 等待无反馈 | 2–4s 内出现 filler 占位 |
| 模型开始回答 | Thinking 继续刷标题 | 进入 Answering 后 Thinking 冻结 |
| 输出收尾 | 不知道过程有效性 | 输出标题统计小结，确认节奏与数量 |

### 1.2 用户需求
- 为支持reasoning参数的模型添加流式思考过程展示
- 为不支持reasoning参数的模型维持现有功能
- 思考过程通过浮动状态指示器展示
- Stage1回答保持选项卡排列，谁先回答好就展示谁的答案
- 思考标题每5秒展示一条
- 不保存用户偏好设置
- 不需要思考标题去重、暂停/继续、导出、多语言支持等功能

## 2. 目标与非目标

### 2.1 目标
1. **内容型 Thinking**：通过 Tool Call 实时输出标题。
2. **只展示标题**：不显示 raw reasoning。
3. **TTFB 填充**：首个标题前输出 filler（2–4s/条，≤6 条）。
4. **流式驱动**：标题必须在 streaming 过程中出现。
5. **最终答案完整**：允许内部推理，最终输出不降质。
6. **状态冻结**：进入 Answering 后拒收任何标题。

### 2.2 非目标
- 不改变现有 Stage1 回答的选项卡排列方式
- 不保存用户偏好设置
- 不需要复杂的动画效果
- 不需要思考标题去重、暂停/继续、导出、多语言支持等功能

## 3. 详细技术方案

### 3.1 通道设计
- **通道 1：tool_calls**
  - `emit_thinking_title(title: string)`：输出标题。
  - `emit_final(final: string)`：输出最终答案（可选，默认不在 prompt 中要求）。
- **通道 2：content 兜底**
  - 最终答案**默认走 content**（可流式）。
  - 若模型意外调用 `emit_final`，客户端仍需兜底接住。

**冻结规则**：
- 一旦进入 Answering（收到 `emit_final` 或 `content`），Thinking 面板冻结。
- 冻结后**不再处理任何标题**（即使后续 tool_calls 仍有 title）。

### 3.2 Tool Call 参数流式拼接
OpenRouter/OpenAI 的 `tool_calls[].function.arguments` 可能分片流式传输，导致半截 JSON。
**正确做法：客户端缓冲拼接，直到 JSON 可完整解析才更新 UI。**

**规则**：
- 对每个 tool_call 使用独立 buffer。
- 每次收到 arguments 增量就追加。
- 仅当 JSON 解析成功，才取出 `title` 更新 UI。
- 不允许渲染半截 JSON。

### 3.3 Filler 策略
- **启动**：请求发出即启动。
- **频率**：每 2–4 秒一条。
- **上限**：最多 3–6 条。
- **停止**：收到首条真实标题后立即停止。
- **保留**：已输出 filler 保留但标记灰色。

## 4. 技术架构图与流程图

### 4.1 整体架构图
```ascii
┌─────────────────────────┐     ┌─────────────────────────┐
│  前端应用                │     │  后端服务                │
│  ┌─────────────────────┐ │     │ ┌─────────────────────┐ │
│  │  会话区域开关按钮     │◄─┐   ┌►│  模型能力检测         │ │
│  ├─────────────────────┤ │ │   │ │                     │ │
│  │  浮动状态指示器       │◄─┼───┼►│  模型调用逻辑         │ │
│  ├─────────────────────┤ │ │   │ │                     │ │
│  │  Stage1选项卡展示     │◄─┘   └►│  思考标题处理         │ │
│  └─────────────────────┘         │                     │ │
│                                 │  最终答案处理         │ │
└─────────────────────────────────┼─────────────────────┘ │
                                  │                     │ │
                                  │  存储服务            │ │
                                  │                     │ │
                                  └─────────────────────┘ │
                                  └─────────────────────────┘
```

### 4.2 思考过程流程图
```ascii
┌─────────────────────────────────────────┐
│ 用户点击"思考过程"开关按钮                 │
└───────────┬─────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│ 检查开关状态：                           │
│ • 开启：调用有reasoning参数的模型        │
│ • 关闭：按照设定调用模型，不传reasoning参数 │
└───────────┬─────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│ 调用模型并检查返回结果：                   │
└───────────┬─────────────────────────────┘
            │
┌───────────┴─────────────────────────────┐     ┌─────────────────────────────────────────┐
│ 开启思考过程时：                          │     │ 关闭思考过程时：                          │
│ 1. 检查模型是否返回思考标题                 │     │ 1. 直接等待模型返回最终答案                │
│ 2. 返回标题：按5秒/条频率展示              │     └─────────────────────────────────────────┘
│ 3. 未返回标题：继续等待最终答案             │
└─────────────────────────────────────────┘
```

## 5. 前端设计方案

### 5.1 会话区域开关按钮设计
```ascii
┌───────────────────────────────────────────────────────────┐
│  会话标题："关于人生意义的探讨"                            │
│                                                           │
│  ┌───────┐  ┌───────┐  ┌───────┐  ┌───────────────────┐  │
│  │  康德  │  │ 特朗普 │  │ 小岛秀夫│  │ 思考过程：[ON] │  │
│  └───────┘  └───────┘  └───────┘  └───────────────────┘  │
└───────────────────────────────────────────────────────────┘
```

### 5.2 浮动状态指示器设计
```ascii
┌───────────────────────────────────────────────────────────┐
│  ┌─────────────────────────────────────────────────────┐  │
│  │  康德: 正在分析上下文...                               │  │
│  │  特朗普: 回顾相关知识库...                             │  │
│  │  小岛秀夫: 正在拆解问题逻辑...                         │  │
│  └─────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────┘
```

### 5.3 Stage1回答展示设计
```ascii
┌───────────────────────────────────────────────────────────┐
│  ┌─────────────────────────────────────────────────────┐  │
│  │  Stage 1: Individual Responses                      │  │
│  │  ┌───────┐  ┌───────┐  ┌───────┐                    │  │
│  │  │  康德  │  │ 特朗普 │  │ 小岛秀夫│                    │  │
│  │  └───────┘  └───────┘  └───────┘                    │  │
│  │  ┌─────────────────────────────────────────────┐    │  │
│  │  │  特朗普的回答：...                             │    │  │
│  │  │  （特朗普先回答完成，所以默认展示其回答）         │    │  │
│  │  └─────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────┘
```

### 5.4 前端设计建议
| 设计元素 | 建议方案 |
|----------|----------|
| **思考标题颜色** | • 模型名称：使用主题蓝色（#1E40AF）<br>• 思考内容：使用深灰色（#374151）<br>• 背景：使用浅灰色（#F3F4F6） |
| **思考标题样式** | • 字体大小：14px<br>• 行高：1.5<br>• 字体：与现有界面保持一致（可能是Inter或Roboto） |
| **思考过程动画效果** | • 使用简单的淡入效果（transition: opacity 0.3s ease-in-out）<br>• 新增思考标题时使用轻微的上移动画<br>• 不需要复杂的动画效果，保持界面简洁 |

## 6. 后端实现方案

### 6.1 模型调用逻辑
```ascii
┌─────────────────────────────────────────┐
│ 检查开关状态：                           │
│ • 开启：调用有reasoning参数的模型        │
│ • 关闭：按照设定调用模型，不传reasoning参数 │
└───────────┬─────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────┐
│ 调用模型并处理返回结果：                   │
│ 1. 解析tool_calls中的思考标题             │
│ 2. 按5秒/条频率推送思考标题到前端           │
│ 3. 处理最终答案并推送                     │
└─────────────────────────────────────────┘
```

### 6.2 思考标题处理
- **去重处理**：不需要
- **频率控制**：5秒/条
- **存储**：正常显示，不设置保存期限
- **错误处理**：未返回标题时继续等待最终答案

## 7. 需要改动的代码文件与关键代码修改

| 文件 | 修改点 | 关键逻辑 |
| :--- | :--- | :--- |
| `backend/openrouter.py` | 新增 streaming 客户端 | 支持 `stream=True` 的 SSE 解析；输出 tool_calls 与 content delta |
| `backend/council.py` | 新增模型能力检测逻辑 | 检测模型是否支持reasoning参数；根据开关状态决定是否传递reasoning参数 |
| `backend/config.py` | 新增配置项 | 思考标题频率、Filler消息等配置 |
| `frontend/src/ChatInterface.jsx` | 新增开关按钮 | 会话区域开关按钮；控制思考过程展示 |
| `frontend/src/Stage1.jsx` | 新增浮动状态指示器 | 展示思考过程；保持选项卡排列方式 |

**关键修改点（逻辑级）**
1. **Tool Call 参数累积**：对 `tool_calls[].function.arguments` 做 buffer 聚合，JSON parse 成功后才更新 UI。
2. **双通道监听**：同一 chunk 可能含 tool_calls 与 content，需同时处理。
3. **冻结规则**：进入 Answering 后拒收新的 title。
4. **Windows 编码处理**：确保 emoji 不乱码。

## 8. 风险与对策

| 风险 | 影响 | 对策 |
| :--- | :--- | :--- |
| 模型不调用工具 | Thinking 区只有 filler | prompt 强约束；模型不可用则更换 |
| tool_call 参数分片 | UI 乱码或半截 | 追加 buffer，JSON parse 成功才更新 |
| 标题过密 | 影响阅读/耗 tokens | prompt 频率约束 3–10 秒 |
| 输出乱码 | Windows 终端无法显示 emoji | 强制 UTF-8 或提示 `chcp 65001` |